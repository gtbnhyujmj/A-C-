0. cmd
   └── D:
   └── mkdir vicent11
   └── cd vicent11

1. 建立虛擬環境
   └── python -m venv venv
       └── venv/Scripts/activate (第一次用打這個)（Windows）
           venv\Scripts\activate.bat (重開機後要打這個)(以後都打這個 不要上面那個)

2. 安裝 Django
   └── pip install django

3. 建立 Django 專案
   └── django-admin startproject main .

4. 建立 App（例：accounts）
   └── python manage.py startapp index
   └── python manage.py startapp accounts
   └── ...

===

- cmd的部分finish.
- 2025/08/06 已確認可無腦用

===
